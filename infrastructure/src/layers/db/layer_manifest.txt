# knotify-db layer — package manifest
#
# Lists the distribution names of every package provided by this layer.
# `make package` reads this file (and the observability layer's manifest) to
# determine which packages to exclude from a function's pip install so the
# deployment zip stays thin.
#
# Convention: use the distribution name as it appears on PyPI / in
# requirements.txt (e.g. "psycopg2-binary" for psycopg2-binary, "pgvector"
# for pgvector).  The filter script normalises names (lowercase + underscores)
# so "psycopg2-binary" and "psycopg2_binary" are treated as the same package.
#
# Lines starting with "#" and blank lines are ignored.

psycopg2-binary
pgvector
knotify-db
