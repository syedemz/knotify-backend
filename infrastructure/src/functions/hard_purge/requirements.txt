# knotify-hard-purge Lambda runtime dependencies
#
# knotify_obs and knotify_db are supplied by Lambda layers and are
# deliberately excluded from this file (build_package.py strips them).
# psycopg2-binary is provided by the knotify-db layer.
# No additional dependencies beyond the shared layers are needed.
