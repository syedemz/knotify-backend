# anonymize_chat_messages Lambda runtime dependencies
#
# This Lambda only needs boto3 (provided by the Lambda runtime) and
# Python stdlib (os, logging).
#
# No Aurora access (psycopg2 not needed).
# No outbound HTTP to external services.
# No Lambda layers needed — DynamoDB-only, no knotify_obs layer required.
