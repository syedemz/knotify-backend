# cognito_user_state Lambda runtime dependencies
#
# This Lambda only needs boto3 (provided by the Lambda runtime) and
# Python stdlib (logging, os).
#
# No Aurora access (psycopg2 not needed).
# No DynamoDB (no table writes).
# No Lambda layers needed — Cognito IDP calls only, no knotify_obs layer required.
