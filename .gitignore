# Claude Code local configuration — never commit
.claude/

# Terraform working directories and state
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars.json
tfplan
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.venv/
venv/
env/
*.egg-info/
dist/
build/
.eggs/
.scratch/
questions/

# Lambda layer build output (produced by build.sh — not committed)
infrastructure/src/layers/*/python/
infrastructure/src/layers/*-layer.zip
infrastructure/src/layers/*.zip

# Environment and secrets — never commit
.env
.env.*
*.pem
*.key

# Integration test env file written by Terraform local_file (story 5.6)
# Contains EDGE_SECRET — never commit
infrastructure/src/tests/integration/.env.test

# OS artefacts
.DS_Store
Thumbs.db
