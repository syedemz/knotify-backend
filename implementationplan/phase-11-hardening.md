phase: 11
title: Pre-launch hardening
last_updated: 2026-06-10

context_summary: |
  Production-readiness consolidation, applied to BOTH environments. Every hardening change in this phase is wired into dev as well as prod — dev gets the same Secrets Manager-sourced DB credentials, the same OIDC deploy role, the same WAF rule set, the same sg-lambda egress tightening, and the same CloudTrail trail as prod. The deliberate dev/prod asymmetries are limited and narrow: (a) Cognito Advanced Security runs in AUDIT mode in dev and ENFORCED in prod (story 11.4) so devs see the risk signals without being blocked, (b) MFA is OPTIONAL in dev and ON in prod (story 11.5) so dev signup flows don't require TOTP enrollment, and (c) the DB_PASSWORD env-var fallback is retained in dev only (story 11.2) so local development against a non-Secrets-Manager backend keeps working. Per docs/PROD_CUTOVER.md the prod deploy gate is closed, so all prod-side applies are produced as terraform plans and consumed by the dev-side apply for validation; stories complete on dev-applied + prod-planned. Migrates DB credentials from Lambda environment variables to Secrets Manager with a rotation Lambda and a Secrets Manager VPC endpoint (§4.4, §7.6 of architecture.md). Switches GitHub Actions from static IAM keys (v1 choice from §3.2) to OIDC federation. Enables Cognito Advanced Security Features (§7.5, §13 #1). Tightens WAF rules and security-group egress on sg-lambda. Enables CloudTrail with root-API alerting. This phase has no business-logic dependencies of its own — it touches existing modules from phases 2, 3, 4, 5 and bumps them to prod-ready posture.

## Carryovers from phase 6
- 1/30-day `username` rename rate limit (architecture §5.7) — DB-side UNIQUE constraint shipped in phase 6.0a; API-side rate limit must land here. Source: phase-6 story 6.0a.
- Per-route throttling on hot routes (architecture §13 / Brainstorm Md5/Q12) — phase 6 stories 6.1–6.4 ship default HTTP API stage throttling only (burst=10/rate=25 from phase-5 story 5.1); per-route overrides for /v1/profiles?username=, /v1/friend-requests, /v1/blocks land here. Source: phase-6 story 6.7.
- Post-apply `e2e-dev` CI job in `.github/workflows/deploy.yml` — phase 6 stories 6.1–6.7 shipped 4 domain integration test files (test_profile/friends/bookmarks/blocks.py), RLS integration (test_rls_enforcement.py), and the end-to-end suite (test_domains_e2e.py), all gated by `@pytest.mark.integration` and skip-gated on `EXECUTE_API_ENDPOINT` / `COGNITO_USER_POOL_ID` / `DB_HOST` env vars. Today the apply-dev job ends after `terraform apply` and the `.env.test` written by `local_file.integration_test_env` (dev/main.tf:530) is never consumed by CI; e2e tests can only run manually. Add an `e2e-dev` job that runs after `apply-dev`, sources `.env.test` for EXECUTE_API_ENDPOINT/DISTRIBUTION_DOMAIN_NAME/EDGE_SECRET, injects the missing Cognito/Aurora vars (COGNITO_USER_POOL_ID, COGNITO_CLIENT_ID, DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD) from GitHub Actions secrets, and runs `pytest infrastructure/src/tests/integration/ -m integration`. Source: phase-6 story 6.7 user follow-up (2026-06-10).

stories:
  - id: 11.1
    title: Secrets Manager module with VPC endpoint
    agent: backenddeveloper
    done: false
    depends_on: []
    acceptance_criteria:
      - infrastructure/modules/secrets/main.tf creates aws_secretsmanager_secret "knotify/db" with KMS encryption, an aws_vpc_endpoint of service "com.amazonaws.${region}.secretsmanager" attached to the private subnets from phase 1 and sg-lambda
      - Module output: db_secret_arn, secrets_endpoint_id
      - terraform validate passes
      - The module is consumed by both environments/dev/ and environments/prod/; dev gets applied, prod gets a clean terraform plan artifact (no apply while the prod gate is closed)
    notes: ""

  - id: 11.2
    title: Aurora credential migration to Secrets Manager
    agent: backenddeveloper
    done: false
    depends_on: [11.1]
    acceptance_criteria:
      - The aurora module is updated so master_password is generated by a random_password resource and stored in the Secrets Manager secret from 11.1
      - The shared Aurora-access layer (phase 3 story 3.3) reads DB credentials from Secrets Manager when the env var DB_SECRET_ARN is set, falling back to the env var DB_PASSWORD for backwards compatibility with dev (which can opt into either)
      - All domain Lambdas have their environment variables updated: DB_SECRET_ARN points at the secret ARN in BOTH environments; DB_PASSWORD env var removed in prod, retained as optional in dev for local-bypass development
      - Dev Aurora is migrated end-to-end: the dev cluster's master_password is rotated to the random_password value and dev Lambdas connect through Secrets Manager from this story onward
      - Integration test in dev: a Lambda invocation reads the password from Secrets Manager and connects successfully
      - Rotation: aws_secretsmanager_secret_rotation with rotation_lambda_arn pointing at a new Lambda src/functions/rotate_db_secret/ that follows the AWS rotation Lambda pattern; rotation interval 30 days; the rotation is enabled in dev (not just configured)
      - Integration test in dev: trigger a rotation, observe a new password takes effect and an existing Lambda invocation continues to succeed (using the cached or refreshed secret)
    notes: ""

  - id: 11.3
    title: GitHub Actions OIDC migration
    agent: backenddeveloper
    done: false
    depends_on: []
    acceptance_criteria:
      - Terraform in a new infrastructure/modules/github_oidc/ module creates an aws_iam_openid_connect_provider for token.actions.githubusercontent.com and aws_iam_role "github-actions-deploy" in the dev account NOW, with a parameterized definition that will produce the same provider + role in the prod account when the prod gate opens (the prod-side terraform plan must show clean creation under docs/PROD_CUTOVER.md step 3); trust policy scoped to the repository's main and development branches (sub claim filter)
      - The .github/workflows/deploy.yml file (from phase 0 generalized for full deploys) and smoke-test.yml are updated to use aws-actions/configure-aws-credentials with role-to-assume=<role-arn> and aws-region; the AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY secret blocks are removed from the dev environment
      - The prod environment-scoped secret blocks remain absent (per phase 0; the prod env is still empty); no static prod IAM user exists to delete
      - After the migration, the static access key configured in phase 0 is deleted from the dev IAM user; the dev IAM user itself is removed
      - A test deploy through the updated workflow against dev completes successfully using OIDC
    notes: "Prod-side OIDC provider + role creation is bundled into the cutover checklist so it lands atomically when prod is brought online."

  - id: 11.4
    title: Cognito Advanced Security Features (AUDIT in dev, ENFORCED in prod)
    agent: backenddeveloper
    done: false
    depends_on: []
    acceptance_criteria:
      - The advanced_security_mode variable in the cognito module (placeholder from phase 4 story 4.4) is flipped to "AUDIT" in dev and "ENFORCED" in prod
      - The dev change is applied; describe-user-pool against the dev pool confirms UserPoolAddOns.AdvancedSecurityMode=AUDIT
      - The prod change is captured in a terraform plan only (prod gate closed); the plan diff is reviewed and committed alongside the dev apply
      - Risk-based authentication action settings (NotifyAction, AccountTakeoverRiskConfiguration) are configured per AWS documented defaults in BOTH environment configurations
    notes: "AUDIT-in-dev is the intentional asymmetry — dev still receives risk telemetry without blocking developer signins."

  - id: 11.5
    title: MFA configuration in Cognito (OPTIONAL in dev, ON in prod)
    agent: backenddeveloper
    done: false
    depends_on: [11.4]
    acceptance_criteria:
      - The cognito module variable mfa_configuration is set to "OPTIONAL" in dev and "ON" in prod, with software_token_mfa_configuration.enabled=true and sms_mfa_configuration removed (no SMS per §7.5)
      - The dev change is applied; describe-user-pool against the dev pool confirms MfaConfiguration=OPTIONAL and SoftwareTokenMfaConfiguration.Enabled=true
      - The prod change is captured in a terraform plan only (prod gate closed)
      - An integration test in dev: a newly signed-up user CAN enroll TOTP MFA (the option is available) but is NOT forced to; sign-in succeeds without MFA
      - Reference for prod cutover: the same module flipped to "ON" in prod will force TOTP enrollment for new prod signups
    notes: "OPTIONAL-in-dev is the intentional asymmetry — devs aren't forced through TOTP for every signup but the code path is exercised."

  - id: 11.6
    title: WAF rule tuning and sg-lambda egress tightening
    agent: backenddeveloper
    done: false
    depends_on: []
    acceptance_criteria:
      - The WAF web ACL from phase 5 story 5.4 gains: an IP reputation list rule (AWSManagedRulesAmazonIpReputationList), a tighter rate-based rule for unauthenticated paths (auth endpoints capped at 100 req per 5 min per IP), and a configurable geo-block list (var.waf_geo_block defaulting to empty)
      - The WAF changes are applied in dev and captured in a prod terraform plan
      - sg-lambda outbound rules from phase 1 story 1.2 are replaced with explicit egress entries in BOTH environments: 443 to the Cognito + AppSync + Secrets Manager VPC endpoint CIDRs only, 5432 to sg-aurora only, plus a single 443 to 0.0.0.0/0 for Expo Push (Expo's IP range is documented but not narrowed in v1)
      - The sg-lambda tightening is applied in dev; the "all egress allowed" placeholder is removed from the module source so dev and the future prod apply both produce the locked-down posture
      - A unit test (.tftest.hcl) asserts the new SG egress rules
      - Integration test in dev: a Lambda invocation succeeds at reaching Aurora (5432) and the VPC-endpointed services (443) but a curl to an arbitrary internet host (other than the Expo push URL) from inside the Lambda fails — proving the tightening landed
    notes: ""

  - id: 11.7
    title: CloudTrail and root-API alerting
    agent: backenddeveloper
    done: false
    depends_on: [10.1]
    acceptance_criteria:
      - infrastructure/modules/cloudtrail/main.tf creates an aws_cloudtrail "knotify-${var.environment}" trail writing to an S3 bucket with object lock + lifecycle to Glacier after 30 days
      - The module is consumed in dev (applied) and in the prod environment config (terraform plan only while the prod gate is closed)
      - aws_cloudwatch_log_metric_filter detects root account usage and unusual API calls (DeleteTrail, StopLogging, DisableKey) and triggers an alarm to the SNS topic from phase 10 story 10.1
      - Integration test in dev: an admin-driven API call simulating a flagged action triggers a published SNS message
    notes: ""

  - id: 11.8
    title: End-to-end hardened deploy smoke test (dev)
    agent: backenddeveloper
    done: false
    depends_on: [11.2, 11.3, 11.4, 11.5, 11.6, 11.7, 11.9]
    acceptance_criteria:
      - A full deploy through the OIDC-based GitHub Actions workflow to dev succeeds without using static keys (the dev IAM user key from phase 0 is already deleted per 11.3)
      - A test user signed up in dev CAN optionally enroll TOTP MFA and signs in cleanly without it (OPTIONAL mode per 11.5)
      - A Lambda invocation in dev connects to Aurora using Secrets Manager-sourced credentials (validated by reading the Lambda log line that prints the secret_arn but not the password)
      - The dev CloudTrail trail shows the deploy actions logged
      - A `terraform plan` against environments/prod/ for the full hardened stack produces a non-empty creation plan with zero errors and is committed as docs/PROD_CUTOVER_plan.txt for the cutover record
    notes: "The prod-equivalent of this story moves to docs/PROD_CUTOVER.md and runs at gate-open time."

  - id: 11.9
    title: Evaluate RDS Proxy adoption for PROD ONLY (decision story — implement in prod or document deferral)
    agent: backenddeveloper
    done: false
    depends_on: [11.2]
    acceptance_criteria:
      - Scope is explicit and non-negotiable RDS Proxy is a prod-only consideration. Dev keeps direct Lambda → Aurora connections through the Secrets Manager-sourced endpoint (Aurora's own DNS endpoint, not a proxy). Rationale dev has near-zero idle traffic and runs with min_acu=0 (scale-to-zero); the ~$15/month per AZ proxy cost is unjustified at dev traffic levels, and the failover-latency/connection-pooling wins RDS Proxy provides do not apply to a single-developer dev environment. Asymmetry recorded in the phase-11 context_summary table of intentional dev/prod differences.
      - A short decision memo at docs/decisions/0001-rds-proxy.md captures (a) current connection-count metrics from CloudWatch for the prod Aurora cluster (DatabaseConnections max + p95 over the prior 7 days — once prod has traffic; otherwise the memo records that prod is pre-launch and the decision must be based on projected v1 traffic from the architecture's capacity assumptions), (b) Lambda cold-start frequency for the top three DB-touching functions (knotify-match, knotify-blocks, AppSync resolvers), (c) the documented DR/failover SLO for the data layer (today implicit "best effort" — confirm or tighten), (d) the cost delta of adding RDS Proxy in prod only (~$15/month per AZ × number of AZs the proxy lives in) versus the connection-headroom and failover-latency wins it buys. The memo MUST NOT include dev-side metrics as inputs — dev is not a candidate for proxy adoption.
      - Decision recorded in the memo as one of three outcomes (1) implement now in prod — proceed to the implementation criteria below; (2) defer with explicit re-evaluation trigger (e.g. "revisit when prod DatabaseConnections p95 exceeds 60% of cluster max for 7 consecutive days" or "revisit when DR SLO is tightened to <30s endpoint-stable failover"); (3) reject permanently with rationale
      - If decision is (1) implement now — infrastructure/modules/rds_proxy/main.tf creates an aws_db_proxy targeting the prod Aurora cluster, IAM auth disabled, credentials sourced from the Secrets Manager secret created in story 11.1, attached to the private subnets from phase 1 and sg-aurora; an aws_db_proxy_default_target_group with connection_pool_config.max_connections_percent tuned to leave 20% headroom on the Aurora connection limit; an aws_db_proxy_target wiring the proxy to the cluster
      - If decision is (1) — the module is consumed in environments/prod/ ONLY. environments/dev/ does not instantiate the rds_proxy module — no terraform resource for the proxy is created or planned in dev. The prod-side terraform plan is produced as a clean artifact (prod gate closed, per docs/PROD_CUTOVER.md); the dev terraform plan must show zero rds_proxy resources to confirm dev was left out by construction.
      - If decision is (1) — the shared Aurora-access layer (phase 3 story 3.3) is updated so the host returned from the Secrets Manager secret resolves to the proxy endpoint in prod and the cluster writer endpoint in dev. The mechanism is either (a) environment-conditional terraform that writes a different host value into the Secrets Manager secret per environment, or (b) a per-env Lambda env var DB_HOST_OVERRIDE that the layer prefers when set. Either way the Python code in the shared layer is environment-agnostic — it just reads the host from the secret and connects.
      - If decision is (1) — integration test deferred to prod cutover (per docs/PROD_CUTOVER.md), since the proxy does not exist in dev. The cutover checklist gains a new step a Lambda invocation in prod through the proxy succeeds, and a forced Aurora failover (modify-db-cluster --apply-immediately failover-db-cluster) is observed to not require any Lambda restart or env-var change. The phase-11 dev deliverable is the clean prod terraform plan plus the memo plus a passing terraform validate on environments/dev/ (proving dev was unaffected).
      - If decision is (2) or (3) — no module is created; the memo plus a one-line status update to architecture.md §13 #29 (recording the deferral trigger or rejection rationale) is the deliverable
    notes: "Decision story, not pre-committed implementation. Architecture §6.4 line 356 ('RDS Proxy added when traffic justifies it') is the input; the memo is the explicit re-evaluation at hardening time. Depends on 11.2 because the proxy would consume the same Secrets Manager secret that 11.2 creates and rotates. 11.8 depends on 11.9 so the end-to-end smoke test runs against the final connection topology — but note the smoke test runs in dev and dev never uses the proxy, so the smoke test only verifies dev's direct-Aurora path stayed working; prod-side proxy validation is owned by the cutover checklist."
