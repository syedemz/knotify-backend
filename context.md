# knotify-backend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Phase 9 (account deletion) — stories 9.1, 9.2, 9.3, 9.4, 9.5, 9.6, 9.7, 9.8, 9.9, and 9.11 complete. Branch feat/phase-9-account-deletion open; PR #148 against development.

## Active blockers
- Production deploys are PAUSED. The prod AWS account has not been provisioned. All phases plan-against-prod but only apply-against-dev. See `docs/PROD_CUTOVER.md` for the full pause mechanism and the flip-on checklist.

## Critical design decisions
None yet.

## Recent changes
- 2026-06-20: Phase 9 story 9.9 complete — deletion_initiator Lambda (infrastructure/src/functions/deletion_initiator/handler.py): extracts user_id from JWT sub, calls states:StartExecution with {user_id, jwt_sub, purge_immediately}, returns 202 with executionArn; GET /v1/profile/me/deletion-status stubbed at 501 (9.10 extensible); runs OUTSIDE VPC; deletion_initiator IAM role (states:StartExecution+DescribeExecution scoped to state machine/execution ARNs, DescribeExecution pre-declared for 9.10); infrastructure/modules/deletion_initiator/ TF module (ARM64, 30s, STATE_MACHINE_ARN env var); dev env wired (separate aws_apigatewayv2_integration + DELETE /v1/profile/me + GET /v1/profile/me/deletion-status routes + lambda_permission; STATE_MACHINE_ARN added to .env.test); prod env fully mirrored (all phase-9 deletion Lambda modules 9.2–9.9 + step_functions + deletion_initiator + API Gateway routes + iam_roles inputs); aurora attribute bugs fixed in both envs (app_user_secret_name→literal, cluster_port→port on hard_purge+soft_delete_aurora); 2 integration tests (skip-gated): bodyless DELETE via CloudFront asserts 202+valid executionArn+WAF non-blocking, DescribeExecution asserts execution exists; 10 Python unit tests pass, 3 deletion_initiator TF tests pass, 78 IAM TF tests pass, terraform validate clean dev+prod; issue #143 closed.
- 2026-06-20: Phase 9 story 9.11 complete — hard_purge Lambda (infrastructure/src/functions/hard_purge/handler.py): scheduled mode deletes users WHERE deleted_at IS NOT NULL AND deleted_at < NOW() - INTERVAL '30 days'; per-user mode (user_id input) drops 30-day window but retains soft-delete guard (WHERE user_id=:id AND deleted_at IS NOT NULL), returns rows_affected=0 cleanly when user not soft-deleted; Aurora ON DELETE CASCADE confirmed on all 5 tables (siblings/friendships/friend_requests/bookmarks/blocks); runs INSIDE VPC; reuses aurora_writer IAM role + knotify_db layer + existing Secrets Manager credential pattern; infrastructure/modules/hard_purge/ TF module (ARM64, 300s timeout, vpc_config required, EventBridge daily rule + target + lambda_permission); both dev and prod environments wired (module.hard_purge → module.iam_roles.role_arns["aurora_writer"]); return shape {rows_affected, mode: "scheduled"|"per_user"}; 9 Python unit tests pass, 8 module TF tests pass; issue #145 closed.
- 2026-06-20: Phase 9 story 9.2 complete — validate_deletion_request Lambda (infrastructure/src/functions/validate_deletion_request/handler.py): checks user_id==jwt_sub (raises UserIdMismatch typed exception), queries account_deletion_audit for in-progress deletion via single PK Query + in-memory classification (raises DeletionInProgress if initiated row exists with no terminal event), writes 'deletion_initiated' record via direct DDB PutItem (not via write_audit_log Lambda — keeps dependency one-way), returns {validated: True, audit_event_id: <uuid4>}; runs OUTSIDE VPC (DynamoDB-only); expire_at as Number 7-year TTL; validate_deletion_request IAM role added to iam_roles module (AWSLambdaBasicExecutionRole + dynamodb:Query+PutItem on account_deletion_audit only, outside VPC); infrastructure/modules/validate_deletion_request/ TF module created (ARM64, 30s timeout, no VPC config); 17 Python unit tests pass; 3 module TF tests pass; 74/74 IAM TF tests pass (4 new); commit ddb4b5e; issue #136 closed.
- 2026-06-20: Phase 9 story 9.7 complete — delete_dynamodb_personal_data Lambda (infrastructure/src/functions/delete_dynamodb_personal_data/handler.py): queries Notifications (PK=user_id, SK=created_at_notification_id) and PushNotificationTokens (PK=user_id, SK=device_id) with LastEvaluatedKey pagination, BatchWriteItem-deletes all rows in ≤25-item chunks; idempotent on empty result sets; returns {notifications_deleted, push_tokens_deleted}; runs OUTSIDE VPC; delete_dynamodb_personal_data IAM role added to iam_roles module (AWSLambdaBasicExecutionRole + dynamodb:Query+BatchWriteItem on Notifications and PushNotificationTokens only, outside VPC); infrastructure/modules/delete_dynamodb_personal_data/ TF module created (ARM64, 300s timeout, no VPC config); 12 Python unit tests pass; 3 module TF tests pass; 70/70 IAM TF tests pass (4 new); issue #141 closed.
- 2026-06-20: Phase 9 story 9.6 complete — anonymize_chat_messages Lambda (infrastructure/src/functions/anonymize_chat_messages/handler.py): Queries ChatMessages PK=room_id with FilterExpression on sender_id, UpdateItem each matching row to set sender_id='[deleted-user]' (content preserved); continuation token {room_ids, current_room_index, last_evaluated_key} with has_more flag for Step Functions Choice→Task→Choice loop; empty room_ids returns has_more=False immediately; re-invocation resumes from current_room_index and uses last_evaluated_key as ExclusiveStartKey; runs OUTSIDE VPC; anonymize_chat_messages IAM role added to iam_roles module (AWSLambdaBasicExecutionRole + dynamodb:Query+UpdateItem on ChatMessages only, outside VPC); infrastructure/modules/anonymize_chat_messages/ TF module created (ARM64, 900s timeout, no VPC config); 18 Python unit tests pass; 3 module TF tests pass; 66/66 IAM TF tests pass (4 new); issue #140 closed.
- 2026-06-20: Phase 9 story 9.5 complete — soft_delete_aurora Lambda (infrastructure/src/functions/soft_delete_aurora/handler.py): §11.1 step-2 UPDATE nulls email/phone_number/photo_url/chosen_profile_avatar/preferences/preference_vector and sets username='[deleted-user]'/first_name='Deleted'/last_name='User'/deleted_at=NOW(); WHERE guard deleted_at IS NULL makes it idempotent (rows_affected=0 on re-run); uses knotify_db.get_connection + existing aurora_writer IAM role (VPC + app-user credential); infrastructure/modules/soft_delete_aurora/ TF module created (vpc_config required, ARM64, 30s timeout, db layer + obs layer); 7 Python unit tests pass, 2 integration tests skip-gated; terraform validate clean; issue #139 closed.
- 2026-06-20: Phase 9 story 9.4 complete — deactivate_chat_rooms Lambda (infrastructure/src/functions/deactivate_chat_rooms/handler.py): queries ChatRoomMembership PK=user_id for all room_ids; conditional UpdateItem on each ChatRooms row (status=deactivated, reason=user_deleted_account, deactivated_at=NOW(); ConditionalCheckFailedException skip preserves first-writer-wins so 'blocked' reason is not overwritten); BatchWriteItem-deletes user's ChatRoomMembership rows in ≤25-item chunks (25-item cap honoured; survivor rows untouched; already-deleted rows tolerated); returns {room_ids:[...]} for state machine downstream injection; deactivate_chat_rooms IAM role added to iam_roles module (AWSLambdaBasicExecutionRole + dynamodb:Query on ChatRoomMembership + dynamodb:UpdateItem on ChatRooms + dynamodb:BatchWriteItem on ChatRoomMembership, outside VPC); infrastructure/modules/deactivate_chat_rooms/ TF module created; 15 Python unit tests pass, 3 module TF tests pass, 62/62 IAM TF tests pass (4 new); issue #138 closed.
- 2026-06-21: Phase 9 story 9.3 complete — cognito_user_state Lambda (infrastructure/src/functions/cognito_user_state/handler.py): single handler dispatches AdminDisableUser (mode=disable) or AdminDeleteUser (mode=delete); idempotent — UserNotFoundException + already-disabled NotAuthorizedException treated as success on disable; UserNotFoundException treated as success on delete; cognito_user_state IAM role added to iam_roles module (AWSLambdaBasicExecutionRole, outside VPC, inline cognito-idp:AdminDisableUser+AdminDeleteUser+AdminGetUser scoped to user_pool_arn); infrastructure/modules/cognito_user_state/ TF module created; 14 Python unit tests pass, 3 module TF tests pass, 58/58 IAM TF tests pass (5 new); issue #137 closed.
- 2026-06-20: Phase 9 story 9.8 complete — account_deletion_audit DynamoDB table (PK user_id/SK event_id, TTL on expire_at Number, SSE, no GSI, PAY_PER_REQUEST) added to infrastructure/modules/dynamodb/; write_audit_log Lambda handler (infrastructure/src/functions/write_audit_log/handler.py) writes deletion_initiated/deletion_completed/deletion_failed records with per-event fields (dynamodb_retention=permanent_anonymized|hard_deleted on completed, failed_state_name+error+cause on failed, expire_at=int(time.time())+7*365*86400); write_audit_log IAM role added to iam_roles module (AWSLambdaBasicExecutionRole + dynamodb:PutItem on account_deletion_audit only, outside VPC); 16 Python unit tests pass, DynamoDB TF 35/35, IAM TF 53/53; commit 080626d; issue #142 closed.
- 2026-06-20: Phase 9 story 9.1 complete — Step Functions state machine module: infrastructure/modules/step_functions/ (main.tf, variables.tf, outputs.tf, account_deletion.asl.json.tftpl) with STANDARD type state machine, soft-delete + purge-immediately branches, global Catch→RecordFailureAuditLog→EmitDeletionFailedMetric, logging at ALL with 7-day CW log group; dedicated stepfn_deletion_exec IAM role added to infrastructure/modules/iam_roles/ (lambda:InvokeFunction on all 9 task ARNs, cloudwatch:PutMetricData, logs:* on log group); 6 new step_functions TF tests pass, 5 new iam_roles TF tests pass (48 total); terraform validate + fmt clean on both modules.
- 2026-06-19: Phase 8 CLOSEOUT — E2E probe (`scripts/probe_phase8.py`) passes all 19 assertions end-to-end against the live dev environment. Closeout followed a five-hotfix sweep that surfaced latent defects the unit/TF suites had missed: PR #130 AppSync `user_pool_config.default_action` DENY→ALLOW (additional auth providers cannot coexist with DENY); PR #131 attach the five client-facing AppSync UNIT resolvers (createOrGetRoom/sendMessage/markAsRead/listMyRooms/messagesByChatRoom) which had been declared but never wired; PR #132 chat_resolver `_handle_create_or_get_room` snake_case→camelCase + persist `createdAt` (schema contract was being violated; tests asserted on the bug); PR #133 truncate sendMessage `ClientRequestToken` to 32 chars (DDB caps at 36) + migration 0016 grants `DELETE` on friendships/friend_requests/blocks to app_user (migration 0007 had withheld it on the assumption ON DELETE CASCADE would cover; block/unfriend flows need explicit DELETEs); PR #134 fix blocks Lambda `_DELETE_FRIEND_REQUESTS_SQL` column names (requester_id/receiver_id → from_user_id/to_user_id per migration 0005). Probe confirms: createOrGetRoom + idempotency, sendMessage + isolation, listMyRooms ordering, block→deactivate+unfriend+read-only gate, re-friend-blocked 409, unfriend→friendshipActive=false (room row preserved per design), @aws_iam directive enforcement. Also added four open questions to phase-9 PRD (oq-9.A through oq-9.D) — all resolved. Index + phase-8 PRD last_updated bumped; tagged phase-8-complete on the development merge commit.
- 2026-06-18: Phase 8 story 8.9 complete — blocks Lambda extended for chat-room deactivation/reactivation with friendship_active flag; blocks_writer_dynamodb IAM policy switched from hardcoded ARN to var.chat_rooms_table_arn conditional; 2 new unit tests (26 total); 1 new TF test (25 total); 3 integration tests skip-gated; issue #120.
- 2026-06-18: Phase 8 story 8.9a complete — room_state_publisher Lambda (DDB Streams → AppSync IAM publish): ChatRooms DDB stream enabled; handler runs OUTSIDE VPC; room_state_publisher IAM role added; NONE-datasource local resolvers for _publishRoomDeactivated/_publishRoomReactivated added; 13 unit tests pass, 4 TF tests pass, 21/21 appsync TF tests pass, 29/29 iam_roles TF tests pass; issue #120 closed.
- 2026-06-18: Phase 8 story 8.9b complete — friends Lambda extended for friendship_active flag lifecycle; friends_writer IAM role added; dev+prod switched from aurora_writer to friends_writer; 16 new unit tests pass (52 total); 4 integration tests skip-gated; 29/29 IAM TF tests pass; issue #121 closed.
- 2026-06-18: Phase 8 story 8.9c complete — notifications_publisher Lambda (DDB Streams → AppSync IAM publish); notifications_publisher IAM role added; 13 unit tests pass; 5 TF tests pass; 33/33 IAM TF tests pass; 23/23 appsync TF tests pass; issue #122 closed.
- 2026-06-18: Phase 8 story 8.10 complete — push_fanout Lambda (DDB Streams → Expo Push API); dual ESM; push_fanout IAM role; prod Secrets Manager expo credential; 12 unit tests pass; 6 TF tests pass; 37/37 IAM TF tests pass; issue #123 closed.
- 2026-06-18: Phase 8 story 8.11 complete — POST /v1/push-tokens REST endpoint; push_tokens IAM role; route wired in dev+prod; 10 unit tests pass; 6 TF tests pass; 40/40 IAM TF tests pass; issue #124 closed.
- 2026-06-18: Phase 8 stories 8.12+8.13 complete — stale_token_cleanup scheduled Lambda + end-to-end chat integration test (skip-gated); 43/43 + 8 Python tests pass; issues #125 #126 closed.
