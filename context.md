# knotify-backend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Phase 5 — API edge (HTTP API + CloudFront + WAF). PRD revised on 2026-06-05 after the phase-5 brainstorm: story 5.0 (us_east_1 provider alias) added, ACM + Route 53 cycle resolved by bundling cert+validation in 5.2, edge-secret enforcement pinned to the in-Lambda helper pattern (option b from B1). Phase 4 fully closed: PR #53 squash-merged 2026-06-01 (9af5ae0); follow-up hotfix PRs #54–#57 (terraform fmt + Cognito TOTP enable + tflint cleanup + db_migrator retry budget widening) merged into development; phase-4-complete git tag pushed.

## Active blockers
- Production deploys are PAUSED. The prod AWS account has not been provisioned. All phases plan-against-prod but only apply-against-dev. See `docs/PROD_CUTOVER.md` for the full pause mechanism and the flip-on checklist.

## Critical design decisions
None yet.

## Recent changes
- 2026-05-23: Phase 2 story 2.13 complete — DynamoDB module extended with PushNotificationTokens table; output push_tokens_table_name exposed; 26/26 plan-mode tests pass; terraform fmt/validate clean.
- 2026-05-23: Phase 2 story 2.14 complete — per-environment data-layer wiring; dynamodb tests extended to 28/28; aurora 10/10 green; terraform fmt/validate clean on all 4 directories; cluster-side migration deferred to phase 3 story 3.7.
- 2026-05-23: Phase 2 complete — all 14 stories (2.1–2.14) done. Phase PR open into development. Cluster-side migration run deferred to phase 3 story 3.7.
- 2026-05-24: Phase 2 PR #28 merged into development (commit e1f0e2d), tagged `phase-2-complete`. Follow-up Aurora scale-to-zero PR #29 also merged (commit c203e82).
- 2026-05-24: Phase-3 brainstorm completed. 23 findings raised (2 blockers, 9 major, 5 medium, 6 minor, 1 cross-phase drift); all resolved. Schema ripple shipped: migrations 0002/0007/0008 edited; architecture.md updated; phase-4 PRD updated. Phase-3 PRD now has 8 stories (added 3.0 for VPC endpoints).
- 2026-05-25: Phase 3 story 3.0 complete — VPC endpoints added to networking module (infrastructure/modules/networking/vpc_endpoints.tf): Secrets Manager Interface endpoint (private_dns_enabled=true, both private subnets, sg-vpce SG allows TCP 443 from sg-lambda only) and DynamoDB Gateway endpoint (4 route-table associations: 2 private + 2 db); outputs secretsmanager_vpc_endpoint_id and dynamodb_vpc_endpoint_id added to outputs.tf; 2 new plan-mode tests (runs 7+8); 8/8 networking tests pass; aurora 10/10 and dynamodb 28/28 unaffected; terraform fmt/validate clean.
- 2026-05-26: Phase 3 story 3.1 complete — Lambda Terraform module authored (infrastructure/modules/lambda/{main.tf,variables.tf,outputs.tf,tests/lambda.tftest.hcl,tests/fixtures/dummy.zip}); publish=true + alias "live" pointing at function.version, depends_on log group (brainstorm M8/M9), merge() env defaults POWERTOOLS_SERVICE_NAME/LOG_LEVEL (brainstorm N2), defaults python3.14/arm64/10s/512MB; provider constraint bumped ~> 5.70 → ~> 6.20 across all 4 modules (python3.14 requires provider >= 6.20); networking aws_region.name deprecation fixed (→ .region); 10/10 lambda tests pass; full suite 56/56 (aurora 10 + networking 8 + dynamodb 28 + lambda 10) clean.
- 2026-05-25: Phase 3 story 3.2 complete — shared observability Lambda layer: infrastructure/src/layers/observability/ contains build.sh (pins aws-lambda-powertools==3.29.0, PyJWT[crypto]==2.13.0, requests==2.32.3 for arm64/python3.14), knotify_obs wrapper module (init_logger, correlation_id_middleware, verify_cognito_jwt), README.md documenting verify_cognito_jwt as rarely-used helper; Terraform module infrastructure/modules/layers/observability/{main.tf,variables.tf,outputs.tf} packages layer as aws_lambda_layer_version with compatible_runtimes=["python3.14"] and compatible_architectures=["arm64"]; 9/9 pytest unit tests pass (4 verify_cognito_jwt tests mock requests.get boundary — wrong issuer/audience/expired/valid); 4/4 Terraform tests pass; full suite 60/60 (aurora 10 + networking 8 + dynamodb 28 + lambda 10 + observability-layer 4) clean.
- 2026-05-25: Phase 3 story 3.3 complete — shared Aurora-access Lambda layer: infrastructure/src/layers/db/ contains build.sh (pins psycopg2-binary==2.9.12, pgvector==0.3.6 for arm64/python3.14), knotify_db wrapper module (get_connection, set_rls_context, rls_context context manager); GUC names app.requesting_user_id and app.requesting_user_sex match migration 0007 lines 115-116 verbatim; context manager owns transaction lifetime (BEGIN on entry, COMMIT/ROLLBACK on exit so SET LOCAL GUCs expire cleanly); Terraform module infrastructure/modules/layers/db/{main.tf,variables.tf,outputs.tf} with compatible_runtimes=["python3.14"] and compatible_architectures=["arm64"]; 17/17 pytest unit tests + 3/3 integration tests pass (3 integration tests deselected by pytest -m "not integration"); 4/4 Terraform tests pass; full suite 64/64 (aurora 10 + networking 8 + dynamodb 28 + lambda 10 + observability-layer 4 + db-layer 4) clean.
- 2026-05-26: Phase 3 story 3.4 complete — IAM roles module (infrastructure/modules/iam_roles/{main.tf,variables.tf,outputs.tf,tests/iam_roles.tftest.hcl}); 7 roles: db_migrator (VPC + master-secret GetSecretValue scoped to rds!cluster-<resource_id>-* + app_user-credential Create/Put/Describe), cognito_trigger (VPC + app_user-credential GetSecretValue), aurora_reader/aurora_writer/dynamodb_chat_writer/dynamodb_notifications_writer (VPC + trust only), stepfn_task (states.amazonaws.com trust + VPC); role_arns output map; 13/13 tests pass; full suite 77/77 clean.
- 2026-05-25: Phase 3 story 3.5 complete — local dev tooling: Makefile (package/package-test/db-up/test targets); build_package.py handles layer exclusion via layer_manifest.txt files + pip install + source copy + zipfile creation (pure Python, no system zip); wait_for_db.py polls docker compose healthcheck; smoke_test_package.py verifies package-test output; infrastructure/src/functions/_smoke/ minimal smoke function; infrastructure/src/pytest.ini (testpaths=., python_files=test_*.py, integration marker); infrastructure/src/README.md documenting full workflow; layer_manifest.txt added to both layer dirs; 51/51 unit tests pass (3 integration deselected); 25 new tests for build_package helpers.
- 2026-05-25: Phase 3 story 3.6 complete — cognito_post_confirmation Lambda (built, not wired): handler.py with _normalize_gender (exact mapping table from AC), _parse_birthdate (ISO fromisoformat with ValueError guard), INSERT-only path (UniqueViolation caught for idempotency — ON CONFLICT DO NOTHING cannot be used because Postgres applies SELECT RLS policy to the conflicting row under FORCE RLS when no user GUCs are set); migration 0007 extended with users_insert_own_row (FOR INSERT WITH CHECK true) and users_update_own_row (FOR UPDATE USING+WITH CHECK own user_id) — SELECT-only policy was incorrectly blocking non-owner INSERTs under FORCE ROW LEVEL SECURITY; Terraform wiring in dev/prod main.tf instantiates module.cognito_post_confirmation, module.observability_layer, module.db_layer, module.iam_roles; DB_SECRET_NAME uses friendly Secrets Manager name (not ARN wildcard); provider version bumped ~> 5.70 → ~> 6.20 in both env backend.tf files; terraform plan shows 30 resources to add, no aws_cognito_user_pool_lambda_config, no aws_lambda_permission for cognito-idp; 28 unit tests + 8 integration tests pass; full suite 79 unit + 11 integration = 90 tests all green.
- 2026-05-25: Phase 3 story 3.7 complete — DB migrator Lambda: yoyo-migrations==9.0.0 added to db layer (build.sh + layer_manifest.txt); build_package.py extended with --include-dir SRC:DEST[:REGEX] flag (_copy_include_dir); Makefile package-all target; db_migrator/handler.py (lazy imports, _generate_password 32-char, _write_app_user_secret CreateSecret→PutSecretValue idempotency, _alter_role_password, _apply_migrations via yoyo API, structured JSON response); 17 unit tests + 5 integration tests; Terraform wiring in dev/prod main.tf (module "db_migrator" + null_resource db_migrator_invoke with migrations_hash+lambda_version triggers); null provider added to both backend.tf; lambda module function_version output added; migration 0007 header comment corrected; pytest.ini addopts = --import-mode=importlib (fixes handler.py naming collision between db_migrator and cognito_post_confirmation in full test suite); 106 unit tests pass; prod at-flip behavior documented in prod/main.tf; PR #38 open. Phase 3 all 8 stories done.
- 2026-05-29: Phase 4 story 4.1 complete — Cognito User Pool Terraform module (infrastructure/modules/cognito/{main.tf,variables.tf,outputs.tf,tests/cognito.tftest.hcl}): aws_cognito_user_pool with email username_attributes, OPTIONAL MFA, email-only account recovery, password policy (min 12, upper/lower/numbers/symbols), 4 schema attrs (given_name/family_name/gender/birthdate, all mutable=true required=false) for social IDP forward-compat, preferred_username absent, user_pool_add_ons advanced_security_mode defaulting to AUDIT (brainstorm B2), token validity variables (access=1h, id=1h, refresh=30d) and unit locals for story 4.2 app clients; outputs user_pool_id/user_pool_arn/user_pool_endpoint; provider ~> 6.20; 10/10 cognito tests + 87/87 full suite pass; terraform fmt/validate clean; branch feat/phase-4-cognito.
- 2026-05-29: Phase 4 story 4.2 complete — Cognito app clients added to cognito module (infrastructure/modules/cognito/app_clients.tf): production SRP-only public client (knotify-${env}-app, generate_secret=false, ALLOW_USER_SRP_AUTH+ALLOW_REFRESH_TOKEN_AUTH, prevent_user_existence_errors=ENABLED, 60m/60m/30d token validity); dev-only integration-test client (knotify-${env}-integration-test, count = env=="dev" ? 1 : 0, ALLOW_ADMIN_USER_PASSWORD_AUTH+ALLOW_REFRESH_TOKEN_AUTH for boto3); outputs app_client_id (always populated) and integration_test_app_client_id (try([0].id, "") — empty string in prod); 3 new plan-mode tests (runs 10-12); 13/13 cognito tests + 90/90 full suite pass; terraform fmt/validate clean; commit 2594706.
- 2026-06-01: Phase 4 story 4.3 complete — post-confirmation trigger wired to Cognito User Pool: added post_confirmation_lambda_arn variable to cognito module variables.tf; lambda_config { post_confirmation = var.post_confirmation_lambda_arn } block added to aws_cognito_user_pool.this in main.tf (M3 resolution — no standalone aws_cognito_user_pool_lambda_config resource); aws_lambda_permission.cognito_post_confirmation_invoke grants cognito-idp.amazonaws.com lambda:InvokeFunction scoped to User Pool ARN (source_arn) to prevent confused-deputy escalation; file-level variables block added to test file for sentinel ARN; 2 new plan-mode tests (runs 14-15 asserting lambda_config.post_confirmation and lambda_permission principal/source_arn); 15/15 cognito tests pass; dev/prod module.cognito blocks added in both environment main.tf files wiring post_confirmation_lambda_arn = module.cognito_post_confirmation.function_arn; terraform validate clean on both environments.
- 2026-05-29: Phase 4 story 4.4 complete — PreTokenGeneration V2 Lambda: infrastructure/src/functions/cognito_pre_token_generation/handler.py queries users.profile_complete_verified by sub and sets custom:profile_complete on BOTH idTokenGeneration and accessTokenGeneration claimsToAddOrOverride (B1); missing row defaults to "false" with structured warning, no raise (Md2); DB exceptions caught, logged, claim defaults to "false" (never raises — raising blocks login); cognito module extended with pre_token_generation_config { lambda_version = "V2_0", lambda_arn = var.pre_token_generation_lambda_arn } and aws_lambda_permission.cognito_pre_token_generation_invoke; dev/prod main.tf both wire module.cognito_pre_token_generation with cognito_trigger IAM role + observability/db layers + private VPC subnets + DB_SECRET_NAME; Makefile package-all extended; 8/8 unit tests + 17/17 Terraform plan-mode tests pass; terraform validate clean; build zip verified 2402 bytes containing handler.py; README.md documents one-DB-hit-per-token contract, cold-start budget, V2 shape, missing-row branch.
- 2026-05-29: Phase 4 story 4.5 complete — per-environment advanced_security_mode wiring: advanced_security_mode variable (default "AUDIT", type string) added to dev/variables.tf and prod/variables.tf; module.cognito call in dev/main.tf and prod/main.tf now explicitly passes advanced_security_mode = var.advanced_security_mode; comment in both main.tf files marks AUDIT as the V2 PreTokenGeneration minimum (story 4.4 / brainstorm B2), ENFORCED upgrade deferred to phase 11 (architecture.md §13 #1); terraform validate clean on both environments; 17/17 cognito plan-mode tests pass including advanced_security_mode_default_is_audit and advanced_security_mode_override_accepted.
- 2026-05-29: Phase 4 story 4.6 complete — E2E signup integration test at infrastructure/src/tests/integration/test_cognito_signup.py: drives real Cognito sign_up (knotify-test+<uuid>@example.com) against dev User Pool, admin_confirm_sign_up fires post-confirmation trigger, asserts Aurora users row with user_id=sub and NULL profile columns (expected bootstrap state), admin_initiate_auth via dev-only ADMIN_USER_PASSWORD_AUTH client asserts custom:profile_complete="false" on BOTH id_token and access_token (B1 two-token contract via PreTokenGeneration Lambda), teardown calls admin_delete_user and DELETE FROM users WHERE user_id=sub via master credential from Secrets Manager (Md4); marked @pytest.mark.integration; boto3/psycopg2 imported inside test body so collection works in docker-compose unit-test env; 92 unit tests pass unchanged, 21 integration tests collected; phase-4 PR ready to open.
- 2026-06-01: Phase 4 PR #53 squash-merged into development as 9af5ae0. The squash-merge commit body inlined `[skip ci]` markers from the per-story bookkeeping commits, which suppressed the auto-deploy push trigger; manual workflow_dispatch run 26748405385 then failed at the `terraform fmt` check due to a 3-line alignment regression in modules/cognito/tests/cognito.tftest.hcl introduced by the file-level `variables` block in story 4.3. Hotfix PR fix/phase-4-fmt → development applies `terraform fmt -recursive` and carries the phase-4-complete bookkeeping that wasn't in the squashed PR.
- 2026-06-02: Phase 4 follow-up hotfixes merged into development — PR #54 (terraform fmt + phase-4-complete bookkeeping carry-over), PR #55 (drop dead token-validity vars/locals to clear tflint), PR #56 (enable TOTP so mfa_configuration=OPTIONAL is accepted by Cognito), PR #57 (widen db_migrator retry budget to absorb Aurora cold boot). `phase-4-complete` git tag pushed against origin/development.
- 2026-06-05: Phase 5 brainstorm completed (phasebrainstorms/phase-5-api-edge-brainstorm.md). 18 findings — 2 blockers (B1 edge-secret enforcement mechanism, B2 ACM/Route 53 cycle + missing us_east_1 provider alias), 6 majors (M1 JWT audience list, M2 forwarded_values deprecation + dev cert branch, M3 WAF rule posture, M4 test URL threading, M5 phase-4.5->4.6 drift, M6 Route 53 count idiom), 5 mediums, 5 minors. All resolved in the revised PRD: story 5.0 added for us_east_1 provider alias; story 5.2 self-contains cert + validation records + validation wait; story 5.6 ships a shared `require_edge_secret` helper in the observability layer (HTTP API keeps its built-in JWT authorizer); phase-6 PRD gained story 6.0 to own the /v1/_internal/hello cleanup.
- 2026-06-08: Phase 5 story 5.0 complete — `provider "aws" { alias = "us_east_1", region = "us-east-1" }` block added to dev/main.tf and prod/main.tf, mirroring primary provider's default_tags; terraform validate clean on both environments (pre-existing DynamoDB hash_key deprecation warnings only, not introduced here); commit 8eab4e8.
- 2026-06-05: Phase 5 story 5.1 complete — HTTP API Gateway Terraform module at infrastructure/modules/api_gateway/: aws_apigatewayv2_api (protocol_type=HTTP), $default stage with configurable throttling (dev defaults burst=10/rate=25), CloudWatch access-log group (retention=7d), Cognito JWT authorizer with compact() audience pattern; 10/10 plan-mode tests pass; terraform fmt/validate clean on dev and prod; module not yet invoked from any env main.tf (env-level wiring deferred to story 5.7).
- 2026-06-05: Phase 5 story 5.2 complete — ACM certificate module at infrastructure/modules/acm/: self-contained cert + DNS validation records + validation wait; count=0/empty-map dev path (zero resources); configuration_aliases=[aws.us_east_1] with provider=aws.us_east_1 on cert and validation resources; try() outputs (empty string dev path); module wired in dev/main.tf and prod/main.tf with providers={aws=aws, aws.us_east_1=aws.us_east_1} and domain_name=var.domain_name; dev.tfvars sets domain_name="" hosted_zone_id=""; docs/PROD_CUTOVER.md §4b added; 8/8 plan-mode tests pass; terraform fmt/validate clean on both environments; commit f645a60.
- 2026-06-05: Phase 5 story 5.3 complete — CloudFront distribution module at infrastructure/modules/cloudfront/: aws_cloudfront_distribution behind HTTP API Gateway with origin_id="api-gateway"; random_password.edge_secret (length=64, alphanumeric-only) injected as x-knotify-edge-secret custom_header; Managed-CachingDisabled + Managed-AllViewer data-source policies (no forwarded_values); viewer_certificate branches on domain_name (dev→cloudfront_default_certificate, prod→acm+sni-only+TLSv1.2_2021); price_class=PriceClass_100; four outputs (distribution_id, distribution_domain_name, distribution_arn, edge_secret sensitive=true); hashicorp/random ~>3.6 added to both env backend.tf and lock files; module.api_gateway wired in dev/main.tf and prod/main.tf (env-level wiring deferred from story 5.1); module.cloudfront wired in both envs; api_gateway throttling variables added to both env variables.tf; 15/15 plan-mode tests pass; terraform fmt/validate clean on both environments.
- 2026-06-05: Phase 5 story 5.4 complete — WAF web ACL module at infrastructure/modules/waf/: aws_wafv2_web_acl scope=CLOUDFRONT using provider=aws.us_east_1 with configuration_aliases=[aws.us_east_1]; 4 rules: CommonRuleSet (priority 1, count), KnownBadInputsRuleSet (priority 2, none/enforce), SQLiRuleSet (priority 3, count), rate-based per-IP (priority 10, block at 2000/5min); all rules + ACL-level visibility_config with cloudwatch_metrics_enabled=sampled_requests_enabled=true; aws_wafv2_web_acl_association attaches ACL to CloudFront distribution ARN; outputs web_acl_id and web_acl_arn; module wired in dev/main.tf and prod/main.tf with providers={aws.us_east_1=aws.us_east_1} and cloudfront_distribution_arn=module.cloudfront.distribution_arn; 10/10 tests pass; terraform fmt/validate clean on both environments.
- 2026-06-05: Phase 5 story 5.5 complete — Route 53 module at infrastructure/modules/route53/: aws_route53_record A-alias pointing custom domain at CloudFront; count=(domain_name==""||hosted_zone_id=="")?0:1 zero-resource dev path; alias.evaluate_target_health=false; record_fqdn output uses try([0].fqdn,""); module wired in dev/main.tf (empty strings → no-op) and prod/main.tf (vars from prod.tfvars at cutover); cloudfront_hosted_zone_id="Z2FDTNDATAQYW2" wired as literal; 10/10 plan-mode tests pass (dev path, both half-configured states, prod path); terraform fmt/validate clean on both environments.
- 2026-06-05: Phase 5 story 5.6 complete — EdgeSecretRequired exception + require_edge_secret(event) + @with_edge_secret decorator added to knotify_obs._edge_secret and exported from knotify_obs.__init__; 13 new unit tests (cases a–g per AC) all pass; full observability suite 22/22 green; hashicorp/local ~>2.5 added to required_providers in dev/backend.tf and prod/backend.tf; local_file.integration_test_env resource added to dev/main.tf and prod/main.tf writing EXECUTE_API_ENDPOINT/DISTRIBUTION_DOMAIN_NAME/EDGE_SECRET to infrastructure/src/tests/integration/.env.test; .env.test added to .gitignore (line 49, verified with git check-ignore); lock files updated for hashicorp/local 2.9.0 (linux_amd64+windows_amd64); terraform validate clean (exit 0) on both envs; observability layer README.md created documenting canonical @with_edge_secret usage and Lambda EDGE_SECRET injection pattern; commit 38ef489.
