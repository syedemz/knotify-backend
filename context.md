# knotify-backend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Phase 8 — stories 8.0–8.9, 8.9a, 8.9b, 8.9c, 8.10 done. Branch feat/phase-8-chat open. Story 8.11 (POST /v1/push-tokens) next.

## Active blockers
- Production deploys are PAUSED. The prod AWS account has not been provisioned. All phases plan-against-prod but only apply-against-dev. See `docs/PROD_CUTOVER.md` for the full pause mechanism and the flip-on checklist.

## Critical design decisions
None yet.

## Recent changes
- 2026-06-17: Phase 8 story 8.6 complete — scoped subscription pipeline resolvers: 1 NONE datasource (NoneDS) + 2 APPSYNC_JS pipeline functions (check_room_membership on ChatRoomMembership DDB datasource: DDB GetItem request/response — Unauthorized on miss; check_identity_match on NONE datasource: JS identity.sub non-empty guard, dual-layer with @aws_subscribe field filter) + 7 PIPELINE resolvers (onMessageInRoom/onTypingInRoom/onRoomDeactivated/onRoomReactivated/onReadReceipt → check_room_membership; onNotificationForMe/onFriendRequestUpdated → check_identity_match); 7 new Terraform module tests (tests 9–15 in appsync.tftest.hcl) + 3 integration tests skip-gated on APPSYNC_GRAPHQL_URL (test_subscriptions_8_6.py); 15/15 TF tests pass; 66 Python unit tests pass + 3 integration tests skip; terraform validate clean dev+prod; issue #116 closed.
- 2026-06-17: Phase 8 story 8.7 complete — markAsRead resolver: _handle_mark_as_read in handler.py; membership GetItem gate then TransactWriteItems (PutItem MessageReads PK=room_id/SK=user_id with last_read_message_id+last_read_at; UpdateItem ChatRoomMembership PK=user_id/SK=room_id with same cached values); returns MessageRead dict matching schema.graphql type (roomId in response drives onReadReceipt subscription field filter); @require_profile_complete_appsync applied; 9 new unit tests pass (46 total); 2 integration tests skip-gated in test_markAsRead.py; issue #117 closed.
- 2026-06-17: Phase 8 story 8.8 complete — setTyping APPSYNC_JS PIPELINE resolver (no storage): choice (a) — two pipeline functions in main.tf: check_room_membership (reused from 8.6, read-only DDB GetItem) + set_typing_passthrough (new, NoneDS, zero write ops); Python _handle_set_typing fallback in handler.py (membership GetItem + TypingEvent {userId,isTyping,roomId} return, no writes); 6 new unit tests (52 total); 3 new TF tests (18 total, all pass); 2 integration tests skip-gated in test_setTyping.py; terraform validate clean dev+prod; issue #118 closed.
- 2026-06-18: Phase 8 story 8.9 complete — blocks Lambda extended for chat-room deactivation/reactivation with friendship_active flag: handler.py _deactivate_chat_room UpdateExpression gains #fa=:false (friendship_active=false on block); _reactivate_chat_room does not touch friendship_active (stays false until 8.9b); blocks_writer_dynamodb IAM policy switched from hardcoded arn:aws:dynamodb:*:*:table/ChatRooms to var.chat_rooms_table_arn conditional (story 8.9 AC-2); 2 new unit tests (26 total, all pass); 1 new TF test (25 total, all pass); 3 integration tests authored + skip-gated in test_blocks_chat_integration.py; terraform validate clean dev+prod; Makefile package-all already targets blocks.zip (no new target needed); IAM role extended: blocks_writer.
- 2026-06-18: Phase 8 story 8.9a complete — room_state_publisher Lambda (DDB Streams → AppSync IAM publish): ChatRooms DDB stream enabled (NEW_AND_OLD_IMAGES) in modules/dynamodb/main.tf + chat_rooms_stream_arn output; handler.py runs OUTSIDE VPC (AppSync public DNS, hotfix #106 lesson), consumes MODIFY events, detects active↔deactivated status transitions, calls _publishRoomDeactivated/_publishRoomReactivated mutations via SigV4 botocore signing; room_state_publisher IAM role in iam_roles module (AWSLambdaBasicExecutionRole, scoped DDB stream actions, appsync:GraphQL on exact field ARNs); appsync api_arn output added; EventSourceMapping batch_size=10/LATEST in modules/room_state_publisher Terraform module (no vpc_config); NONE-datasource local resolvers for _publishRoomDeactivated + _publishRoomReactivated added to appsync module; dev+prod root modules wired; Makefile package-all extended; 13 unit tests pass, 4 room_state_publisher TF tests pass, 21/21 appsync TF tests pass, 29/29 iam_roles TF tests pass; 2 integration tests skip-gated; issue #120 closed.
- 2026-06-18: Phase 8 story 8.9b complete — friends Lambda extended for friendship_active flag lifecycle: handler.py gains _get_dynamo, _set_friendship_active (accept path: SET #fa=:true, attribute_exists condition), _clear_friendship_active (unfriend path: SET #fa=:false, attribute_exists condition, no status touch); knotify_obs.chat_room_id imported; friends_writer IAM role added to iam_roles module (VPC + app_user_credential + dynamodb:UpdateItem on ChatRooms only) and exported in outputs.tf; dev+prod module "friends" switched from aurora_writer to friends_writer role + TABLE_CHAT_ROOMS env var injected from module.dynamodb.chat_rooms_table_name; 16 new unit tests pass (52 total, all pass); 4 integration tests authored + skip-gated; 29/29 IAM TF tests pass; terraform validate clean dev+prod; issue #121 closed.
- 2026-06-18: Phase 8 story 8.9c complete — notifications_publisher Lambda (DDB Streams → AppSync IAM publish): handler.py runs OUTSIDE VPC, consumes Notifications stream INSERT events, dispatches by type (friend_request_accepted → _publishFriendRequestUpdated, all other known generics → publishNotification, unknown → warn+skip); payload includes userId (PK) so onNotificationForMe/onFriendRequestUpdated identity filters work; NONE-datasource resolvers for publishNotification + _publishFriendRequestUpdated added to appsync module; notifications_publisher IAM role in iam_roles (AWSLambdaBasicExecutionRole + DDB stream read on Notifications stream ARN + appsync:GraphQL on exact field ARNs); ESM batch_size=10/LATEST; dev+prod root modules wired; Makefile package-all extended; 13 unit tests pass; 5 notifications_publisher TF tests pass; 33/33 IAM TF tests pass; 23/23 appsync TF tests pass; 3 integration tests skip-gated; terraform validate clean dev+prod; issue #122 closed.
- 2026-06-18: Phase 8 story 8.10 complete — push_fanout Lambda (DDB Streams → Expo Push API): handler.py runs OUTSIDE VPC (hotfix #106 lesson); dual ESM on ChatMessages + Notifications streams (batch_size=10, LATEST); ChatMessages INSERT path: GetItem ChatRooms → determine recipient → GetItem ChatRoomMembership (notifications_muted gate) → Query PushNotificationTokens → POST Expo per token (title=cached_other_name, body[:80], deepLink=knotify://chat/<room_id>); NO Notifications write from chat path (§5.4.2); Notifications INSERT path: Query tokens → POST Expo → mark delivered on HTTP 200; DeviceNotRegistered → DeleteItem stale token; EXPO_AUTH_MODE env var (none|bearer) read at call time via _get_expo_auth_mode(); prod bearer token from Secrets Manager on cold start; push_fanout IAM role (AWSLambdaBasicExecutionRole, scoped DDB stream + table actions, scoped SM GetSecretValue on expo secret); modules/push_fanout Terraform module + tests; aws_secretsmanager_secret in prod root module; dev+prod root modules wired; Makefile push_fanout target; 12 unit tests pass; 6 push_fanout TF tests pass; 37/37 IAM TF tests pass; terraform validate clean dev+prod; issue #123 closed.
