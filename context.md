# knotify-backend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Phase 6 — Profile, friends, bookmarks, blocks domain Lambdas — ALL 10 STORIES DONE on `feat/phase-6-domain-lambdas`. PR #82 open against development. Stories shipped: 6.0 (hello stub removed), 6.0a (migration 0010 case-insensitive partial UNIQUE on lower(username)), 6.0b (`knotify_obs._chat_room_id` + `_blocks` shared helpers), 6.1 (knotify-profile Lambda + 4 routes + `completed_profile_user` fixture), 6.2 (knotify-friends Lambda + 7 routes + block-aware POST/GETs + lex-min/max friendship canonical ordering), 6.3 (knotify-bookmarks Lambda + 3 routes + idempotent POST), 6.4 (knotify-blocks Lambda + `blocks_writer` IAM role + DynamoDB ChatRooms deactivate/reactivate + `chat_deactivation_pending` on lag), 6.5 (route wiring regression sweep: `test_route_wiring.py` 35 tests across 17 routes), 6.6 (`test_rls_fail_closed.py` docker-compose unit + `test_rls_enforcement.py` integration + `app_user_conn` fixture with ResourceNotFoundException skip-gate), 6.7 (`test_domains_e2e.py` + Makefile `test-e2e` + idempotent carryover writes to `phase-11-hardening.md` and `phase-8-chat.md`). Dev infra remains destroyed (run 27188014855 from 2026-06-09); CI exercises integration tests on PR merge / next apply. Next handoff: review and merge PR #82 into development (squash per `gitbranching.md`); then `git fetch origin && git tag phase-6-complete origin/development && git push origin phase-6-complete`; then review the phase-7 (match) PRD and flip phase 7 `ready: true` in the thin index.

## Active blockers
- Production deploys are PAUSED. The prod AWS account has not been provisioned. All phases plan-against-prod but only apply-against-dev. See `docs/PROD_CUTOVER.md` for the full pause mechanism and the flip-on checklist.

## Critical design decisions
None yet.

## Recent changes
- 2026-06-09: Phase 6 story 6.1 complete — knotify-profile Lambda: GET /v1/profile/me (full profile, RLS own-row), PATCH /v1/profile/me (immutable-field 400 before DB trigger, idempotent re-PATCH 200, profile_complete_verified flip in same txn), GET /v1/profiles?username (case-insensitive lower() match, deck-view), GET /v1/profiles/{userId} (deck-view, 404 on RLS hide); 18 unit tests pass; completed_profile_user(sex) fixture added to integration conftest.py (3-step: PATCH + Aurora assert + fresh admin_initiate_auth); integration test file test_profile.py authored for CI; Terraform module "profile" in dev+prod, 4 JWT-authorised routes, aurora_writer role gets GetSecretValue policy, Makefile extended; terraform validate clean on both envs. Commit 3ad4922.
- 2026-06-09: Phase 6 story 6.4 complete — knotify-blocks Lambda (audit-and-finish): GET /v1/blocks, POST /v1/blocks (friendship guard, single Aurora txn: INSERT blocks + DELETE friendship + DELETE friend_requests, DynamoDB deactivation with ConditionExpression + ConditionalCheckFailedException no-op + chat_deactivation_pending flag), DELETE /v1/blocks/{userId} (unblock + DynamoDB reactivation, same error contract); blocks_writer IAM role (VPC + scoped GetSecretValue + scoped dynamodb:UpdateItem on ChatRooms); dev+prod Terraform wiring (module "blocks" + integration + 3 JWT routes + permission, TABLE_CHAT_ROOMS from dynamodb module, EDGE_SECRET); 24/24 unit tests pass; test_blocks.py integration test authored (positive: seed friendship + ChatRooms row, POST block asserts deactivation, DELETE unblock asserts reactivation; negative: 409 not_friends, blocks count unchanged, no ChatRooms row); terraform validate clean dev+prod; make package FUNC=blocks produces blocks.zip; handler docstring updated (chat_deactivation_pending naming decision, empty user_sex rationale); IAM module tests 14/15/16 added for blocks_writer role.
- 2026-06-10: Phase 6 story 6.2 complete — knotify-friends Lambda (audit-and-finish): fixed missing global _conn in _handle_delete_friend (broken connection was not cleared on DB error); removed dead POST→DELETE-request branch in _dispatch; 36/36 unit tests pass (including new regression test for global _conn reset path); terraform validate clean dev+prod; friends.zip produced; integration test (test_friends.py, 2 tests: AC-HAPPY + AC-BLOCK) collects cleanly; dev+prod Terraform wiring (module "friends" + integration + 7 JWT routes + aurora_writer role + permission, EDGE_SECRET). Commit 88c73e2.
- 2026-06-10: Phase 6 story 6.3 complete — knotify-bookmarks Lambda: GET /v1/bookmarks (block-filtered via block_filter("bookmarks.bookmarked_user_id")), POST /v1/bookmarks (idempotent ON CONFLICT DO NOTHING, is_blocked inside transaction), DELETE /v1/bookmarks/{userId} (idempotent 200); aurora_writer IAM role; 24/24 unit tests pass; integration test (test_bookmarks.py, 2 tests: AC-HAPPY + AC-BLOCK) collects cleanly; dev+prod Terraform wiring (module "bookmarks" + integration + 3 JWT routes + permission, EDGE_SECRET); Makefile package-all extended; terraform validate clean dev+prod; bookmarks.zip produced.
- 2026-06-10: Phase 6 story 6.5 complete — route-wiring regression sweep: test_route_wiring.py (35 tests: 1 route-existence/JWT-auth invariant + 17 parametrized 401-unauthenticated-via-CF + 17 parametrized 403-authenticated-via-execute-api-no-edge-secret); zero new TF resources; pytest --collect-only clean; terraform plan zero-diff invariant deferred to CI post-apply plan-only job.
- 2026-06-10: Phase 6 story 6.6 complete — RLS GUC enforcement tests: test_rls_fail_closed.py (docker-compose, 3 assertions: fail-closed 0 rows / Male-A sees 2 / Female-C sees 3, all pass); test_rls_enforcement.py (integration, 2 tests: AC-RLS-HIDE + AC-RLS-SELF, env-var skip-gated for CI); app_user_conn fixture added to conftest.py (ResourceNotFoundException skip-gate for pre-migrator state); zero TF changes.
- 2026-06-10: Phase 6 story 6.7 complete — end-to-end suite test_domains_e2e.py (1 test: profile read/update + friend request/accept/list + bookmark add/list/remove + block/409/unblock/re-request); Makefile test-e2e target; carryover writes to phase-11-hardening.md and phase-8-chat.md. Commit 7e46b2b.
- 2026-06-10: Phase 6 COMPLETE — all 10 stories done (6.0, 6.0a, 6.0b, 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 6.7); thin implementationplan.md index phase-6 done: true; PR #82 body updated with Tracking section linking #72–#81. Awaiting user squash-merge into development, then `git tag phase-6-complete origin/development && git push origin phase-6-complete`. Phase 7 (match and deck) is next — review the phase-7 PRD and flip phase 7 ready: true in the thin index.
