# knotify-backend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Phase 8 — stories 8.0, 8.1, 8.2, 8.3, 8.4, 8.5, 8.6, and 8.7 done. Branch feat/phase-8-chat open. Story 8.8 (setTyping mutation) next.

## Active blockers
- Production deploys are PAUSED. The prod AWS account has not been provisioned. All phases plan-against-prod but only apply-against-dev. See `docs/PROD_CUTOVER.md` for the full pause mechanism and the flip-on checklist.

## Critical design decisions
None yet.

## Recent changes
- 2026-06-10: Phase 6 story 6.6 complete — RLS GUC enforcement tests: test_rls_fail_closed.py (docker-compose, 3 assertions: fail-closed 0 rows / Male-A sees 2 / Female-C sees 3, all pass); test_rls_enforcement.py (integration, 2 tests: AC-RLS-HIDE + AC-RLS-SELF, env-var skip-gated for CI); app_user_conn fixture added to conftest.py (ResourceNotFoundException skip-gate for pre-migrator state); zero TF changes.
- 2026-06-10: Phase 6 story 6.7 complete — end-to-end suite test_domains_e2e.py (1 test: profile read/update + friend request/accept/list + bookmark add/list/remove + block/409/unblock/re-request); Makefile test-e2e target; carryover writes to phase-11-hardening.md and phase-8-chat.md. Commit 7e46b2b.
- 2026-06-10: Phase 6 COMPLETE — all 10 stories done (6.0, 6.0a, 6.0b, 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 6.7); thin implementationplan.md index phase-6 done: true; PR #82 body updated with Tracking section linking #72–#81. Awaiting user squash-merge into development, then `git tag phase-6-complete origin/development && git push origin phase-6-complete`. Phase 7 (match and deck) is next — review the phase-7 PRD and flip phase 7 ready: true in the thin index.
- 2026-06-13: Hotfix #84 merged (commit 69ef6b9) — db secret contract: app_user_credential now holds creds only; AURORA_HOST/PORT/DBNAME injected via Lambda environment_variables on all 6 user-facing Lambdas (profile, friends, bookmarks, blocks, cognito_post_confirmation, cognito_pre_token_generation). knotify_db.get_connection() string-arg path raises EnvironmentError with precise message if any env var missing. 25 unit tests pass.
- 2026-06-13: Hotfix #85 merged (commit 63a9165) — Cognito user pool schema oscillation: AWS provider 6.x alternates adding/removing the four OIDC standard-attribute schema blocks across consecutive applies. lifecycle { ignore_changes = [schema] } added to `aws_cognito_user_pool.this` with a block comment explaining the oscillating-diff root cause. Apply succeeds.
- 2026-06-13: Hotfix #86 merged (commit 1a6118d) — Lambda permission source_arn: switched 4 domain Lambdas' aws_lambda_permission.source_arn from `module.api_gateway.default_stage_arn` (apigateway-management ARN) to a new `api_execution_arn` output (`aws_apigatewayv2_api.this.execution_arn`). API Gateway invokes Lambdas using execute-api ARNs; the old stage ARN caused 5xx with NO Lambda invocation log entry. Output description in modules/api_gateway/outputs.tf documents the trap.
- 2026-06-13: Hotfix #87 merged (commit cab8f23) — two latent SQL bugs in friends/bookmarks: (1) block_filter() correlated subquery referenced bare table names (e.g. `bookmarks.bookmarked_user_id`) while outer queries aliased the table (`FROM bookmarks bk`) — Postgres rejected with "invalid reference to FROM-clause entry". _ALLOWED_COLUMNS now holds alias-qualified entries; callers pass `bk.bookmarked_user_id`, `f.user_a/b`, `fr.from_user_id/to_user_id`, `u.user_id`. (2) friends/handler.py was written against an imagined friend_requests schema. Migration 0005 actually defines (request_id, from_user_id, to_user_id, status, created_at, responded_at), but the handler used (id, requester_id, receiver_id, updated_at). All six SQL statements rewritten; accept handler reads renamed row keys; response shapes now expose request_id/from_user_id/to_user_id (no client in prod yet). New unit test explicitly rejects the legacy bare-table whitelist entries so a regression fails at unit-test time, not at runtime.
- 2026-06-15: End-to-end probe via CloudFront (`d1bbz5xkahlmg9.cloudfront.net`) with a fresh JWT: GET /v1/friends → 200 `{friends: []}`, GET /v1/friend-requests → 200 `{friend_requests: []}`, GET /v1/bookmarks → 200 `{bookmarks: []}`, GET /v1/blocks → 200 `{blocks: []}`. Phase 6 fully verified live in dev. Phase 7 (match and deck) is next — review the phase-7 PRD and flip phase 7 ready: true in the thin index.
- 2026-06-16: Phase 7 story 7.0 complete (commit 7cd7e5b) — knotify-match Lambda scaffold: handler.py (empty dispatcher, 404 for all routes), __init__.py, requirements.txt, tests/ (9 unit tests pass); aurora_reader_match IAM role (VPC + scoped GetSecretValue on app-user-credential, 18/18 IAM module terraform tests pass); module "match" wired in dev+prod main.tf (ARM64, knotify_db+knotify_obs layers, AURORA_HOST/PORT/DBNAME+DB_SECRET_NAME+EDGE_SECRET); Makefile package-all extended; terraform validate clean dev+prod; PR #98 open; issue #89 closed.
- 2026-06-16: Phase 7 story 7.0a complete — migration 0011_deck_view_extended.sql: DROP + CREATE deck_view with 16 columns (original 14 + preference_vector + religion), idx_deck_user UNIQUE recreated, refresh_deck_view() LANGUAGE sql unchanged, GRANT SELECT to app_user; rollback 0011 restores 14-column view; Assertion C added to test_deck_view.py verifying both new columns appear with exact values after refresh.
- 2026-06-16: Phase 7 story 7.0b complete (commit 72fc610) — onboarding enforcement: migration 0012 widens profile_complete CHECK to 34 fields + marriage_time DEFAULT; @require_profile_complete decorator (fail-closed, custom:profile_complete JWT claim) in knotify_obs layer; cognito_pre_token_generation drops Aurora roundtrip, reads Cognito userAttributes from event instead; profile handler widens _REQUIRED_FOR_COMPLETION to 34 fields + best-effort Cognito admin_update_user_attributes after Aurora commit; friends/bookmarks/blocks decorated with @require_profile_complete; Cognito module gets custom:profile_complete schema block; iam_roles gets cognito-idp:AdminUpdateUserAttributes policy on aurora_writer + cognito_user_pool_arn var; dev+prod main.tf wire USER_POOL_ID + cognito_user_pool_arn; 120 unit tests pass; terraform validate clean; issue #91 closed.
- 2026-06-16: Phase 7 story 7.3 complete — preference vector encoder: knotify_db/prefs.py with PREFERENCE_KEYS (20-key §5.5 order) and encode_prefs(); knotify_db/__init__.py exports both; profile PATCH handler imports encode_prefs and injects preference_vector = %s::vector alongside preferences in the same UPDATE when "preferences" is in the patch body; 12 new unit tests (9 prefs + 3 handler) all pass; integration test authored in test_profile.py; profile.zip rebuilt; terraform validate clean dev+prod; issue #94 closed.
- 2026-06-16: Phase 7 story 7.4 complete — deck_view refresh: migration 0013 creates aurora_refresh Postgres role (LOGIN, NOBYPASSRLS) + refresh_log table; db_migrator rotates aurora_refresh password into knotify-<env>-aurora-refresh-credential; new refresh_deck_view Lambda (advisory-lock guard, refresh_deck_view() call, refresh_log INSERT); aurora_refresh_lambda IAM role (VPC + scoped GetSecretValue); EventBridge scheduled rule every 15 minutes in dev+prod; profile PATCH async-invokes refresh Lambda post-commit on flag flip; modules/refresh_deck_view Terraform module; dev+prod root modules wired; Makefile package-all extended; 17 new unit tests (13 refresh + 4 profile) all pass; 2 integration tests authored; issue #95 closed.
- 2026-06-16: Phase 7 story 7.1 complete — POST /v1/match/search: _handle_post_match_search added to match handler with body parsing, _validate_search_body (countries=CHAR(2)-only, religion=str, age_min/max=int, age_min<=age_max), _is_empty_vector (NULL/all-zeros detection), _build_search_sql (cosine ORDER BY <=> when vector non-empty, fallback ORDER BY created_at DESC); queries users aliased as u, u.sex predicate omitted (RLS enforces), block_filter("u.user_id") embedded, LIMIT 50; _dispatch wired for POST /v1/match/search; _ALLOWED_COLUMNS whitelist in _blocks.py extended with "dv.user_id" (for story 7.2) in single edit — "u.user_id" was already present, no duplicate added; 35 new unit tests pass; 2 new tests added to test_blocks.py for dv.user_id whitelist entry; 3 integration tests authored in test_match_search.py (cosine ranking, block filtering, empty-vector fallback); issue #92 closed.
- 2026-06-16: Phase 7 story 7.2 complete — GET /v1/match/deck: _build_deck_sql (FROM deck_view dv, explicit opposite-sex WHERE using current_setting GUC, block_filter("dv.user_id"), optional countries/religion/age filters, optional cursor WHERE dv.user_id > %s::uuid, ORDER BY dv.user_id ASC, LIMIT 20); _parse_deck_filters (URL-decode + reuse _validate_search_body); _handle_get_match_deck (next_cursor = last user_id when 20 rows, null otherwise; rls_context sets app.requesting_user_sex GUC); _dispatch wired for GET /v1/match/deck; stale 7.0/7.1 deck-returns-404 tests updated to reflect route now live; 23 new unit tests pass (67 total in match suite); 3 integration tests authored in test_match_deck.py (verified-only, cursor pagination round-trip, opposite-sex filter); issue #93 closed.
- 2026-06-16: Phase 7 story 7.5 complete (commit 4ea1727) — HTTP API route wiring for match: aws_apigatewayv2_integration.match + aws_apigatewayv2_route.post_match_search (POST /v1/match/search) + aws_apigatewayv2_route.get_match_deck (GET /v1/match/deck) + aws_lambda_permission.match_api_gateway (source_arn uses api_execution_arn per hotfix #86, NOT default_stage_arn); both routes carry authorization_type=JWT + authorizer_id=module.api_gateway.authorizer_id; handler.py already has @with_edge_secret outermost + @require_profile_complete inside (no code change); test_route_wiring.py _EXPECTED_ROUTE_KEYS extended from 17 to 19 routes; terraform validate clean dev+prod; issue #96 closed.
- 2026-06-16: Phase 7 story 7.6 complete — end-to-end match ordering test: seeded_match_candidates fixture added to conftest.py (n=10 opposite-sex candidates with deterministic perturbation vectors giving strict cosine ranking); test_match_e2e.py tests full match path: 403-before/200-after profile completion gate with explicit REFRESH_TOKEN_AUTH token refresh, EXACT cosine ordering on POST /v1/match/search, membership equality on GET /v1/match/deck, refresh Lambda invoked synchronously (RequestResponse), blocked top-ranked candidate absent from both endpoints; 69 integration tests collect + skip cleanly; issue #97 closed. Phase 7 COMPLETE — PR #98 open, awaiting squash-merge into development.
- 2026-06-17: Hotfix #106 merged — VPC endpoints for cognito-idp and lambda services. Probe surfaced that PATCH /v1/profile/me hung for the full 30s timeout when the post-commit `cognito-idp:AdminUpdateUserAttributes` and `lambda:Invoke` (refresh_deck_view) calls SYN-blackholed: private subnets have no NAT egress and the public service hostnames could not be reached. Added two `aws_vpc_endpoint` Interface resources in `infrastructure/modules/networking/vpc_endpoints.tf`, mirroring the existing secretsmanager pattern (same sg-vpce SG, both private subnets, `private_dns_enabled = true`).
- 2026-06-17: Hotfix #107 merged — `_build_search_sql` parameter binding. Cosine-path branch built `ORDER BY u.preference_vector <=> %s::vector` but prepended the vector to params instead of appending. psycopg2 binds positionally, so the vector landed in the religion slot, religion in countries, and every following filter shifted by one — POST /v1/match/search returned 500 even with valid filters. Fix: build params list fully, then `params.append(str(requester_vector))` so the trailing `%s::vector` placeholder lines up. New regression tests assert `sql.count("%s") == len(params)` on both cosine and fallback branches.
- 2026-06-17: Hotfix #108 merged — search self-exclusion. RLS on `users` permits two paths (`sex != requesting_user_sex` OR `user_id = requesting_user_id` — the latter is the self-row escape hatch phase-6 endpoints depend on), so search returning the requester's own row was technically RLS-compliant. Fix: explicit `AND u.user_id != %s::uuid` predicate added to `_build_search_sql` WHERE clause with `user_id` appended as the first element of params. Regression test asserts the predicate is present and the requester id is bound.
- 2026-06-17: Probe-pass — `scripts/probe_phase7.py` ran end-to-end against dev after #109 merged. Two fresh opposite-sex Cognito users (`prober_*` Male requester + `cand_*` Female candidate). Gate closed pre-completion (403 on /v1/match/search), opened post-completion. Requester JWT refresh via REFRESH_TOKEN_AUTH carries `custom:user_sex="Male"` and `custom:profile_complete="true"`. refresh_deck_view invoked synchronously. POST /v1/match/search → 14 results, all `cand_*` Female (zero `prober_*`). GET /v1/match/deck → 14 results, ALL `sex=Female`, requester absent. Confirms claim emitted by pre-token-gen + profile PATCH writer + RLS GUC populated + opposite-sex filter enforced end-to-end. Phase 7 verified live. Side observation: 14 leftover Aurora `cand_*` rows persisted from prior probe runs (Cognito reset removed user pool records but Aurora rows survived — inert because no JWT can match an orphan user_id, but worth a one-shot cleanup before phase 8 wedges chat onto these rows).
- 2026-06-17: Hotfix user-sex-jwt-propagation opened — root cause of the deck-self-leak. All five domain Lambdas (profile, blocks, bookmarks, friends, match) read `claims.get("custom:user_sex", "")` and push the value into the `app.requesting_user_sex` Postgres GUC, but the JWT claim was never emitted by any Cognito trigger. Specified in architecture.md §6, flagged in phase-6 brainstorm finding A.5, but the propagation layer was deferred — phase 6 endpoints all carry the self-row RLS escape hatch so an empty GUC did not visibly break them. Phase 7 is the first phase where opposite-sex filtering is load-bearing: `dv.sex != current_setting('app.requesting_user_sex', true)` degenerates to `dv.sex != ''` with an empty GUC, matching every row including same-sex candidates. Fix bundled four edits: (1) `cognito_pre_token_generation/handler.py` reads the standard Cognito `gender` attribute from `event.request.userAttributes`, normalises Male/Female/m/M/male/f/F/female to canonical "Male"/"Female", injects `custom:user_sex` on both id and access tokens — claim is OMITTED entirely on absent/unrecognised values so consumers log a missing-claim event instead of silently mis-filtering; (2) `profile/handler.py` `_write_cognito_profile_complete` now accepts a `sex` kwarg and bundles `{Name: "gender", Value: sex}` into the same AdminUpdateUserAttributes call that flips `custom:profile_complete` — exactly one round-trip on the flag flip, sex is always available because it is one of the 34 required-for-completion fields; (3) `match/handler.py` `_build_deck_sql` adds defence-in-depth `AND dv.user_id != %s::uuid` predicate so the deck cannot leak the self-row even if the GUC is empty for any future edge case; (4) `infrastructure/src/scripts/backfill_cognito_gender.py` one-shot script scans Aurora `users.sex` for every active user and pushes to Cognito `gender` via AdminUpdateUserAttributes (idempotent, `--dry-run` supported, runs from a developer workstation NOT a Lambda). No Cognito schema change required because `gender` is a standard OIDC attribute already declared on the user pool. Existing dev testers must either have the backfill run against them or trigger a fresh profile PATCH to set Cognito `gender` going forward; next JWT refresh carries the claim. Phase-7 PRD appended with the full hotfix record.
- 2026-06-18: Phase 8 story 8.0 complete — chat_resolver Lambda scaffold: handler.py (empty dispatcher returning Unimplemented for all typeName/fieldName pairs, @require_profile_complete_appsync decorator applied, block_filter() mandate documented); @require_profile_complete_appsync decorator added to knotify_obs layer (both knotify_obs/ and python/knotify_obs/ trees); dynamodb/outputs.tf extended with 6 table-ARN outputs (chat_rooms_arn, chat_room_membership_arn, chat_messages_arn, message_reads_arn, notifications_arn, push_notification_tokens_arn); chat_resolver IAM role in iam_roles module (VPC + scoped GetSecretValue + DynamoDB GetItem/PutItem/UpdateItem/Query/BatchGetItem/TransactWriteItems on 5 chat tables); modules/chat_resolver Terraform module (ARM64, in-VPC, 30s timeout, AURORA_HOST/PORT/DBNAME+DB_SECRET_NAME, exports lambda_arn); dev+prod root modules wired; Makefile package-all extended; 11 unit tests pass; terraform validate clean dev+prod. Branch feat/phase-8-chat.
- 2026-06-17: Phase 8 story 8.1 complete — AppSync Terraform module: infrastructure/modules/appsync/main.tf (AMAZON_COGNITO_USER_POOLS primary + AWS_IAM secondary auth, user_pool_config DENY default, log_config ALL field-level 7-day CW retention, 5 DynamoDB datasources, chat_resolver_ds AWS_LAMBDA datasource, placeholder schema for story 8.2 swap); two new IAM roles in iam_roles module (appsync_logs with AWSAppSyncPushToCloudWatchLogs, appsync_chat_resolver_invoke with lambda:InvokeFunction on chat_resolver Lambda ARN); iam_roles variables.tf + outputs.tf extended; dev+prod root modules wired (module "appsync" + chat_resolver_lambda_arn on iam_roles); 7 appsync module tests pass, 24 IAM roles tests pass; terraform validate clean dev+prod.
- 2026-06-17: Phase 8 story 8.2 complete — hand-written GraphQL SDL: infrastructure/modules/appsync/schema.graphql (Message, ChatRoom, ChatRoomMembership, MessageRead, Notification, TypingEvent types per §5.4; Query skeleton listMyRooms + messagesByChatRoom with MessageConnection pagination wrapper; Mutation surface createOrGetRoom/sendMessage/markAsRead/setTyping + 4 backend-only @aws_iam publish mutations; Subscription type with 7 @aws_subscribe scoped subs — onMessageInRoom/onTypingInRoom/onRoomDeactivated/onRoomReactivated/onReadReceipt bound by roomId, onNotificationForMe/onFriendRequestUpdated identity-scoped); main.tf schema swapped from inline placeholder to file("${path.module}/schema.graphql"); 1 new terraform test (schema_loaded_from_file_contains_canonical_types, 14 assertions); 8/8 appsync tests pass; terraform validate clean dev+prod; issue #112 closed.
- 2026-06-17: Phase 8 story 8.3 complete — createOrGetRoom resolver: _handle_create_or_get_room in chat_resolver/handler.py (SELF_CHAT/BLOCKED/NOT_FRIENDS guards via knotify_obs.is_blocked + inline _is_friend SQL on friendships canonical pair; room_id via knotify_obs.chat_room_id; atomic TransactWriteItems: 1 ChatRooms PutItem + 2 ChatRoomMembership PutItems, all attribute_not_exists conditional; idempotent on TransactionCanceledException/ConditionalCheckFailed via GetItem fallback); REASON_* string constants + _canonical_pair + _unauthorized_response helpers exported for downstream stories; dispatcher wired for (Mutation, createOrGetRoom); 16 unit tests pass; 4 integration tests authored + skip-gated (IT-8.3-4 passes without live env); issue #113 closed.
- 2026-06-17: Phase 8 story 8.4 complete — sendMessage resolver: derive_client_request_token (pure SHA-256 function, epoch_second injected for testability) + hand-rolled ULID generator (no new pip dep, Crockford Base32); _handle_send_message (membership GetItem gate, room status/friendship_active guards, TransactWriteItems with PutItem ChatMessages + UpdateItem ChatRooms last_message_* fields, ClientRequestToken passed, sender_id from identity.sub only, no Notifications row per §5.4.2); dispatcher wired for (Mutation, sendMessage); REASON_ROOM_DEACTIVATED + REASON_ROOM_READ_ONLY constants added; 48 unit tests pass (7 token-purity + 14 sendMessage unit + 16 createOrGetRoom unit + 11 dispatcher/decorator); 4 integration tests authored + skip-gated; issue #114 closed.
- 2026-06-17: Phase 8 story 8.5 complete — listMyRooms + messagesByChatRoom query resolvers: _handle_list_my_rooms (Query ChatRoomMembership PK=sub, BatchGetItem ChatRooms, sort by last_message_at desc, 100-item cap TODO documented); _handle_messages_by_chat_room (GetItem membership gate → Unauthorized, Query ChatMessages ScanIndexForward=False, SK > createdAtGt when supplied, default limit 20 capped at 100, nextToken via _encode_next_token/_decode_next_token base64-JSON pure helpers); _decode_room + _decode_message helpers; dispatcher wired for (Query, listMyRooms) + (Query, messagesByChatRoom); 36 unit tests pass (18 new: 6 pagination helpers + 2 routing + 5 listMyRooms + 5 messagesByChatRoom + 2 decorator gate); 4 integration tests authored + skip-gated in test_chatQueries.py; issue #115 closed.
- 2026-06-17: Phase 8 fixup — ChatMessages SK attribute name realigned to `created_at_message_id` (preempts ValidationException on deployed sendMessage; surfaced by story 8.5 build). handler.py PutItem key (line 616), KeyConditionExpression SK comparison (line 871), _decode_message item.get() key (line 793), and _decode_message docstring updated; 8 occurrences in test_chat_resolver.py updated; 66/66 unit tests pass. Refs #114 #115.
- 2026-06-17: Phase 8 story 8.6 complete — scoped subscription pipeline resolvers: 1 NONE datasource (NoneDS) + 2 APPSYNC_JS pipeline functions (check_room_membership on ChatRoomMembership DDB datasource: DDB GetItem request/response — Unauthorized on miss; check_identity_match on NONE datasource: JS identity.sub non-empty guard, dual-layer with @aws_subscribe field filter) + 7 PIPELINE resolvers (onMessageInRoom/onTypingInRoom/onRoomDeactivated/onRoomReactivated/onReadReceipt → check_room_membership; onNotificationForMe/onFriendRequestUpdated → check_identity_match); 7 new Terraform module tests (tests 9–15 in appsync.tftest.hcl) + 3 integration tests skip-gated on APPSYNC_GRAPHQL_URL (test_subscriptions_8_6.py); 15/15 TF tests pass; 66 Python unit tests pass + 3 integration tests skip; terraform validate clean dev+prod; issue #116 closed.
- 2026-06-17: Phase 8 story 8.7 complete — markAsRead resolver: _handle_mark_as_read in handler.py; membership GetItem gate then TransactWriteItems (PutItem MessageReads PK=room_id/SK=user_id with last_read_message_id+last_read_at; UpdateItem ChatRoomMembership PK=user_id/SK=room_id with same cached values); returns MessageRead dict matching schema.graphql type (roomId in response drives onReadReceipt subscription field filter); @require_profile_complete_appsync applied; 9 new unit tests pass (46 total); 2 integration tests skip-gated in test_markAsRead.py; issue #117 closed.
